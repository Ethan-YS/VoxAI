# ── 敏感信息（绝对不能提交）────────────────────────────────
# 含 HuggingFace Token，用 config.example.json 作模板
config.json

# ── 大体积目录 ────────────────────────────────────────────
# Python 虚拟环境（用户自行安装）
venv/
# 本地模型文件（运行时自动下载）
models/

# ── Xcode 构建产物 ────────────────────────────────────────
VoxSage-App/DerivedData/
build/
*.xcuserstate
xcuserdata/

# ── macOS 系统文件 ────────────────────────────────────────
.DS_Store
.AppleDouble

# ── Python ────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# ── 录音临时文件 ──────────────────────────────────────────
*.wav
*.mp3

# ── 打包产物（走 GitHub Releases）────────────────────────
*.dmg

# ── 内部文档 ──────────────────────────────────────────────
截图/
